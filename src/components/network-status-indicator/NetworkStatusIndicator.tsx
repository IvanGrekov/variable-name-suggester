'use client';

import { useNetworkStatusIndicator } from 'hooks/networkStatusIndicator.hooks';

export default function NetworkStatusIndicator(): null {
    useNetworkStatusIndicator();

    return null;
}
